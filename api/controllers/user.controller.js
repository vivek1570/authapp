const x = (req, res) => {
  res.json({
    message: "Api is not working hello muchu",
  });
};

const y = (req, res) => {
  res.json({
    message: "geti working in getrrrrr",
  });
};

export { x, y };
